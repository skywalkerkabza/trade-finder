# trade-finder
application that lets users search for professionals (plumbers, builders, and electricians) by entering their city. The application will use MySQL to store professional details and Docker Compose to run two containers: one for the Node.js (Express) app and one for the MySQL server.

 docker build -t tradefinder . 
 
docker-compose up --build

